{
  "name": "metronominal-website",
  "version": "1.0.0",
  "private": true,
  "description": "A collaborative multi-track metronome.",
  "scripts": {
    "test": "playwright test",
    "test:sync": "playwright test tests/sync.spec.js"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0"
  }
}
